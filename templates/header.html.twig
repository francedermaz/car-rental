{# header.html.twig #}
<link rel="stylesheet" rel="preload" href="{{ asset('css/header.css') }}">

<header class="header">
    <div class="logo-container">
        <a href="{{ path('app_home') }}" class="logo">
            <img src="{{ asset('images/cars.webp') }}" alt="Logo" class="logo-image">
        </a>
    </div>

    <nav class="nav-links">
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('ordenes_admin') }}" class="reservas-link">Órdenes</a>
        {% else %}
            <a href="{{ path('reservas_usuario') }}" class="reservas-link">Mis Reservas</a>
        {% endif %}
        <a href="{{ path('ver_datos_usuario') }}" class="user-icon">
            <img src="{{ asset('images/user.png') }}" alt="Usuario" class="user-image">
        </a>
    </nav>
</header>